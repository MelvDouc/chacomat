{
  "tasks": {
    "build_npm": "deno run -A scripts/build_npm.ts",
    "test": "clear && deno test -A && deno bench -A"
  },
  "imports": {
    "@/": "./src/",
    "@dev_deps": "./dev_deps.ts"
  },
  "compilerOptions": {
    "noImplicitOverride": true,
    "strict": true
  }
}